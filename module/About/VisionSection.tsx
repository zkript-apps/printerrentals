import React from "react";
import TitleDescription from "./TitleDescription";

const VisionSection = () => {
  return (
    <div>
      <TitleDescription title="VISION">
        <code className="font-sans md:whitespace-pre-wrap">{`
To see our printer rental service gained the trust of 100,000 companies in the 
Philippines in a way that brings glory to God.
        `}</code>
       
      </TitleDescription>
    </div>
  );
};

export default VisionSection;
